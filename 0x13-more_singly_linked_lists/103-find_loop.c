#include "lists.h"
/**
 * find_listint_loop - Find the loop
 * @head: Pointer to the head node.
 *
 * Description: Function finds the loop in the code.
 *
 * Return: The address where the loop starts
 */
listint_t *find_listint_loop(listint_t *head)
{
	listint_t *add;

	add = &listint_t;

	return (add);
}
