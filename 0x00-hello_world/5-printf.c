#include<stdio.h>

/**
 * main - prints a statement using printf() function
 *
 * Description: Program prints out a statement using printf() function
 *
 * Return: returns 0 if successfully executed
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
